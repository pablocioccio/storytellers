import { ExpandTextareaDirective } from './expand-textarea.directive';

describe('ExpandTextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
